# Database Configuration
# Used by docker-compose.yml for backend container
DB_HOST=your-host
DB_PORT=3306
DB_NAME=finance
DB_USER=your-username
DB_PASSWORD=your-password

# JWT Configuration
JWT_SECRET=your-secret-key-at-least-256-bits-long-please-change-this
JWT_EXPIRATION=86400000

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update the values with your actual database credentials
# 3. Generate a secure JWT_SECRET (at least 256 bits / 32 chars)
# 4. Run: docker-compose up -d
# 5. Never commit .env to git (it's already in .gitignore)
