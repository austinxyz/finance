# Database Configuration (External MySQL)
# Format: jdbc:mysql://HOST:PORT/DATABASE?parameters
DB_URL=jdbc:mysql://your-host:your-port/finance?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=your-username
DB_PASSWORD=your-password

# Backend Configuration
# (Add any other environment variables your backend needs)

# Frontend Configuration
# (Add any frontend environment variables)
