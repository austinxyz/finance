# 投资管理需求文档

## 1. 功能概述

### 1.1 目标
为个人理财系统添加投资管理模块，支持投资账户的交易记录、投资分析和回报率计算。

### 1.2 核心功能
- 标识投资类资产账户
- 记录投资交易（投入/取出）
- 批量录入投资交易
- 投资年度分析和回报率计算
- 投资数据可视化

## 2. 数据模型设计

### 2.1 资产分类表修改 (asset_categories)

**新增字段**:
```sql
is_investment BOOLEAN DEFAULT FALSE COMMENT '是否为投资类账户'
```

**投资类资产大类**:
- 股票投资 (STOCKS) - is_investment = TRUE
- 退休基金 (RETIREMENT_FUND) - is_investment = TRUE
- 数字货币 (CRYPTOCURRENCY) - is_investment = TRUE
- 其他可能需要标记的类别

**非投资类资产**:
- 现金 (CASH) - is_investment = FALSE
- 保险 (INSURANCE) - is_investment = FALSE
- 房产 (REAL_ESTATE) - is_investment = FALSE
- 贵金属 (PRECIOUS_METALS) - 可选

### 2.2 投资交易记录表 (investment_transactions)

**表结构**:
```sql
CREATE TABLE investment_transactions (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    account_id BIGINT NOT NULL COMMENT '资产账户ID',
    transaction_period VARCHAR(7) NOT NULL COMMENT '交易期间(YYYY-MM格式)',
    transaction_type ENUM('DEPOSIT', 'WITHDRAWAL') NOT NULL COMMENT '交易类型：投入/取出',
    amount DECIMAL(15,2) NOT NULL COMMENT '交易金额',
    description VARCHAR(500) COMMENT '交易说明',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    FOREIGN KEY (account_id) REFERENCES asset_accounts(id) ON DELETE CASCADE,

    INDEX idx_account_period (account_id, transaction_period)
) COMMENT='投资交易记录表';
```

**字段说明**:
- `transaction_period`: 格式为 "YYYY-MM"，例如 "2024-01"
- `transaction_type`:
  - `DEPOSIT`: 投入（买入、追加投资）
  - `WITHDRAWAL`: 取出（卖出、提取资金）
- `amount`: 正数表示金额，类型由transaction_type决定方向

**衍生字段（通过关联查询获取）**:
- `user_id`: 通过 account_id → asset_accounts → user_id 获取
- `family_id`: 通过 account_id → asset_accounts → user_id → family_members → family_id 获取
- `currency`: 通过 account_id → asset_accounts → currency 获取

### 2.3 与现有表的关系

```
asset_categories (资产分类)
    ↓ is_investment = TRUE
asset_accounts (资产账户)
    ↓
investment_transactions (投资交易)
    ↓ 用于分析
asset_records (资产记录 - 用于获取期初期末值)
```

## 3. 功能模块详细说明

### 3.1 管理-投资管理-分类与记录

**页面路径**: `/management/investment/records`

**页面布局**: 完全参照"支出管理-分类与记录"（ExpenseCategories.vue）的布局设计

#### 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  投资管理 - 分类与记录              [选择家庭: Family 1 ▼]  │
│  管理投资账户，按大类查看投资趋势和交易记录                   │
├─────────────────────────────────────────────────────────────┤
│  📈 股票投资  │  🏦 退休基金  │  💎 数字货币                │  ← 大类Tab（横向）
├─────────┬───────────────────────────────────────────────────┤
│ 投资账户│                                                   │
│ 列表    │  账户信息卡片                                      │
│ (3列)  │  ┌─────────────────────────────────────────────┐ │
│         │  │ 📈 股票投资 - Fidelity 401k                 │ │
│ Fidelity│  │ 持有人: 张三 | 币种: USD                    │ │
│ 401k    │  │ 当前总值: $152,500 (2024-01-31)      [+ 添加│ │
│ $152.5K │  │                                      交易]  │ │
│ 12条记录│  └─────────────────────────────────────────────┘ │
│ [✏️]    │                                                   │
│         │  ┌──────────────────┐  ┌──────────────────────┐ │
│ Charles │  │  投资趋势        │  │  交易历史            │ │
│ Schwab  │  │  [6月][1年][3年] │  │                      │ │
│ $85.2K  │  │  [全部]          │  │  期间  │类型│金额│操作│ │
│ 8条记录 │  │                  │  │ ──────┼────┼────┼──│ │
│ [✏️]    │  │  [折线图         │  │ 2024-01│投入│$2.5K│✏️🗑️│ │
│         │  │   +柱状图]       │  │ 2024-02│投入│$2.5K│✏️🗑️│ │
│ Robinhood│  │                  │  │ 2024-03│取出│$1.0K│✏️🗑️│ │
│ $12.8K  │  │                  │  │                      │ │
│ 3条记录 │  │                  │  │                      │ │
│ [✏️]    │  │                  │  │                      │ │
│         │  └──────────────────┘  └──────────────────────┘ │
│ ─────── │                      (两列等宽布局)             │
│ 股票投资│                                                   │
│ 总计    │                                                   │
│ $250.5K │                                                   │
│ 共3个账户│                                                   │
└─────────┴───────────────────────────────────────────────────┘
```

#### 1. 顶部区域（页面标题和控制）
```html
<div class="flex items-center justify-between">
  <div>
    <h1>投资管理 - 分类与记录</h1>
    <p>管理投资账户，按大类查看投资趋势和交易记录</p>
  </div>
  <div>
    <label>选择家庭:</label>
    <select v-model="selectedFamilyId">...</select>
  </div>
</div>
```

#### 2. 大类Tab区域（水平Tab栏）
```html
<nav class="flex space-x-4 border-b">
  <button
    v-for="category in investmentCategories"
    @click="selectedCategory = category"
    :class="选中样式"
  >
    <span>{{ category.icon }}</span>
    {{ category.name }}
  </button>
</nav>
```

**特点**:
- 横向滚动Tab栏
- 只显示投资类资产大类（`is_investment = TRUE`）
- Tab格式：`图标 + 大类名称`
- 选中Tab底部蓝色下划线

#### 3. 三列Grid布局 (grid-cols-12)

**左侧栏（col-span-3）：投资账户列表**

```html
<div class="col-span-3 bg-white rounded-lg shadow">
  <!-- 标题栏 -->
  <div class="px-4 py-3 border-b flex justify-between">
    <h2>投资账户列表</h2>
    <!-- 无添加按钮 -->
  </div>

  <!-- 账户列表 -->
  <div class="divide-y max-h-[calc(100vh-400px)] overflow-y-auto">
    <div
      v-for="account in accounts"
      @click="selectAccount(account)"
      :class="选中高亮样式"
      class="group relative"
    >
      <div class="px-3 py-2">
        <div class="font-medium">{{ account.accountName }}</div>
        <div class="text-xs text-gray-500">
          {{ account.recordCount }}条记录
          <span v-if="account.latestValue">
            最近${{ account.latestValue }}
          </span>
        </div>
      </div>
      <!-- 悬停显示编辑按钮 -->
      <button class="absolute top-2 right-2 opacity-0 group-hover:opacity-100">
        ✏️
      </button>
    </div>
  </div>

  <!-- 底部汇总 -->
  <div class="px-4 py-3 border-t bg-gray-50">
    <div class="flex justify-between">
      <span>{{ selectedCategory.name }}总计</span>
      <span class="font-bold">${{ categoryTotal }}</span>
    </div>
    <div class="text-xs text-gray-500">
      共 {{ accounts.length }} 个账户
    </div>
  </div>
</div>
```

**右侧区域（col-span-9）：账户详情**

**未选中账户时**:
```html
<div class="col-span-9 bg-white rounded-lg shadow p-12 text-center">
  <svg>...</svg>
  <h3>请选择投资账户</h3>
  <p>从左侧列表选择一个账户查看详情和交易记录</p>
</div>
```

**选中账户后**:

**账户信息卡片**:
```html
<div class="bg-white rounded-lg shadow border p-4 mb-6">
  <div class="flex items-center justify-between">
    <div>
      <h2 class="flex items-center gap-2">
        <span>{{ category.icon }}</span>
        {{ category.name }} - {{ account.accountName }}
      </h2>
      <p>持有人: {{ account.holderName }} | 币种: {{ account.currency }}</p>
    </div>
    <button class="btn-primary" @click="openTransactionDialog()">
      + 添加交易
    </button>
  </div>
</div>
```

**两列子布局（grid-cols-2，等宽）**:

**左列：投资趋势图**:
```html
<div class="bg-white rounded-lg shadow border p-6">
  <div class="flex justify-between mb-4">
    <h3>投资趋势</h3>
    <div class="flex gap-2">
      <button @click="timeRange='6m'">6月</button>
      <button @click="timeRange='12m'">1年</button>
      <button @click="timeRange='36m'">3年</button>
      <button @click="timeRange='all'">全部</button>
    </div>
  </div>
  <div v-if="hasData" class="h-80">
    <canvas ref="chartCanvas"></canvas>
  </div>
  <div v-else class="h-80 flex items-center justify-center">
    暂无数据，请添加记录
  </div>
</div>
```

**图表说明**:
- **折线图**: 账户总值趋势（从`asset_records`获取月末值）
- **柱状图**: 投入/取出交易（从`investment_transactions`获取）
  - 绿色柱：投入（DEPOSIT）
  - 红色柱：取出（WITHDRAWAL）
- 使用Chart.js的混合图表类型

**右列：交易历史记录**:
```html
<div class="bg-white rounded-lg shadow border">
  <div class="px-4 py-3 border-b">
    <h3>交易历史</h3>
  </div>
  <div v-if="loading">加载中...</div>
  <div v-else-if="transactions.length === 0">暂无记录</div>
  <div v-else class="max-h-[480px] overflow-y-auto">
    <table class="w-full">
      <thead class="bg-gray-50 sticky top-0">
        <tr>
          <th>期间</th>
          <th>类型</th>
          <th class="text-right">金额</th>
          <th class="text-right">操作</th>
        </tr>
      </thead>
      <tbody>
        <tr v-for="tx in transactions">
          <td>{{ tx.transactionPeriod }}</td>
          <td>{{ tx.transactionType === 'DEPOSIT' ? '投入' : '取出' }}</td>
          <td class="text-right">
            {{ getCurrencySymbol(account.currency) }}{{ formatNumber(tx.amount) }}
          </td>
          <td class="text-right">
            <button @click="editTransaction(tx)">编辑</button>
            <button @click="deleteTransaction(tx)">删除</button>
          </td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
```

#### 4. 新增/编辑交易弹窗

```html
<div v-if="showDialog" class="fixed inset-0 bg-black/50 flex items-center justify-center">
  <div class="bg-white rounded-lg shadow-xl w-full max-w-md">
    <div class="px-6 py-4 border-b">
      <h3>{{ isEditing ? '编辑交易' : '添加投资交易' }}</h3>
    </div>
    <div class="px-6 py-4 space-y-4">
      <!-- 账户（只读） -->
      <div>
        <label>账户 *</label>
        <input :value="selectedAccount.accountName" readonly class="bg-gray-100" />
      </div>

      <!-- 币种（只读） -->
      <div>
        <label>币种 *</label>
        <input :value="selectedAccount.currency" readonly class="bg-gray-100" />
      </div>

      <!-- 交易期间 -->
      <div>
        <label>交易期间 *</label>
        <input v-model="form.transactionPeriod" type="month" />
      </div>

      <!-- 交易类型 -->
      <div>
        <label>交易类型 *</label>
        <div class="flex gap-4">
          <label class="flex items-center gap-2">
            <input v-model="form.transactionType" type="radio" value="DEPOSIT" />
            <div>
              <span>投入</span>
              <span class="text-xs text-gray-500">(买入、追加投资)</span>
            </div>
          </label>
          <label class="flex items-center gap-2">
            <input v-model="form.transactionType" type="radio" value="WITHDRAWAL" />
            <div>
              <span>取出</span>
              <span class="text-xs text-gray-500">(卖出、提取资金)</span>
            </div>
          </label>
        </div>
      </div>

      <!-- 金额 -->
      <div>
        <label>金额 *</label>
        <div class="relative">
          <span class="absolute left-3 top-2">{{ getCurrencySymbol(selectedAccount.currency) }}</span>
          <input v-model="form.amount" type="number" step="0.01" class="pl-8" />
        </div>
      </div>

      <!-- 说明 -->
      <div>
        <label>说明</label>
        <textarea v-model="form.description" rows="2" maxlength="200"></textarea>
      </div>
    </div>
    <div class="px-6 py-4 border-t flex justify-end gap-3">
      <button @click="closeDialog()">取消</button>
      <button @click="saveTransaction()" class="btn-primary">保存</button>
    </div>
  </div>
</div>
```

#### 5. 交互流程

1. **初始加载**:
   - 加载所有投资大类（`is_investment = TRUE`）
   - 默认选中第一个大类Tab
   - 加载该大类下的所有账户
   - 如果有账户，默认选中第一个

2. **切换大类Tab**:
   - 清空当前选中账户
   - 加载新���类下的账户列表
   - 清空右侧详情区域

3. **选择账户**:
   - 高亮选中账户
   - 加载账户基本信息
   - 加载最近12个月的资产记录（用于折线图）
   - 加载最近12个月的投资交易记录（用于柱状图和列表）
   - 渲染趋势图表

4. **切换时间范围**:
   - 重新加载对应时间范围的数据
   - 更新图表显示

5. **添加/编辑交易**:
   - 点击"+ 添加交易"或"编辑"按钮
   - 弹出表单对话框
   - 填写/修改交易信息
   - 保存后刷新交易列表和图表

#### 6. 数据加载逻辑

**API调用顺序**:
```javascript
// 1. 初始加载
await loadInvestmentCategories()  // 获取投资大类
await loadAccountsByCategory(categoryId)  // 获取该大类下的账户

// 2. 选中账户
await loadAccountDetails(accountId)  // 获取账户详情
await loadAssetRecords(accountId, timeRange)  // 获取资产记录（折线图）
await loadTransactions(accountId, timeRange)  // 获取交易记录（柱状图+列表）

// 3. 渲染图表
updateChart(assetRecords, transactions)
```

#### 7. 关键差异说明

**与支出管理的区别**:
| 项目 | 支出管理 | 投资管理 |
|------|---------|---------|
| 左侧列表 | 子分类列表 | 投资账户列表 |
| 添加按钮 | 添加子分类 | 无（账户在资产管理添加） |
| 编辑功能 | 编辑子分类 | 编辑按钮跳转到资产管理 |
| 记录类型 | 单一支出 | 投入/取出两种类型 |
| 图表数据 | 仅支出金额 | 账户总值 + 投入/取出 |
| 数据来源 | expense_records | asset_records + investment_transactions |

### 3.2 管理-投资管理-批量录入

**页面路径**: `/management/investment/batch-entry`

**页面布局**: 完全参照"支出管理-批量录入"（ExpenseBatchUpdate.vue）的布局设计

#### 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  批量录入投资交易                                             │
├─────────────────────────────────────────────────────────────┤
│  [家庭: Family 1 ▼]  [月份: 2024-01]        [保存全部]      │
├─────────────────────────────────────────────────────────────┤
│  账户               │ 11月  │ 12月  │ 01月  │ 2024年02月    │
│                     │(投入) │(投入) │(投入) │ 投入  │ 取出  │
│ ─────────────────────┼───────┼───────┼───────┼───────┼───────│
│ 📈 股票投资         │       │       │       │       │       │
│   Fidelity 401k     │ $2.5K │ $2.5K │ $2.5K │[$2500]│[$0  ] │
│   Charles Schwab    │ $1.0K │ $1.0K │ $1.0K │[$1000]│[$0  ] │
│ 🏦 退休基金         │       │       │       │       │       │
│   Vanguard IRA      │ $1.5K │ $1.5K │ $1.5K │[$1500]│[$0  ] │
│   Traditional IRA   │   $0  │   $0  │   $0  │[$ 0 ]│[$0  ] │
│ 💎 数字货币         │       │       │       │       │       │
│   Coinbase          │   $0  │   $0  │ $500  │[$ 0 ]│[$500] │
├─────────────────────────────────────────────────────────────┤
│  本月总投入: $5,000      本月总取出: $500                    │
│  净投入: $4,500                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1. 顶部控制区

**控制项**:
```html
<div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
  <!-- 家庭选择器 -->
  <div class="flex items-center gap-2">
    <label>家庭：</label>
    <select v-model="selectedFamilyId">...</select>
  </div>

  <!-- 月份选择器 -->
  <div class="flex items-center gap-2">
    <label>月份：</label>
    <input v-model="transactionPeriod" type="month" />
  </div>
</div>

<!-- 保存按钮 -->
<div class="flex justify-end">
  <button @click="saveAll" :disabled="!hasChanges">
    保存全部
  </button>
</div>
```

#### 2. 投资账户列表表格

**表头结构**:
```
| 账户 | 前3月（投入） | 前2月（投入） | 上月（投入） | 本月投入 | 本月取出 |
```

**数据列说明**:

1. **账户列（col-span-3）**:
   - 显示：大类图标 + 大类名称 + 账户名称
   - 示例：`📈 股票投资 - Fidelity 401k`
   - 按大类分组显示

2. **前3个月列（各占2列，只读，显示投入）**:
   - 显示最近3个月的投入金额（只显示DEPOSIT类型）
   - 只读，灰色文字，用于参考
   - 格式：`$2,500`
   - 目的：帮助用户了解历史投入趋势

3. **本月投入列（col-span-2）**:
   - 输入框，可编辑
   - 带货币符号（根据账户币种）
   - 占位符：`0.00`

4. **本月取出列（col-span-2）**:
   - 输入框，可编辑
   - 带货币符号（根据账户币种）
   - 占位符：`0.00`

**数据行布局**:
```html
<div class="grid grid-cols-12 gap-3 px-4 py-2.5 hover:bg-gray-50">
  <!-- 账户信息 -->
  <div class="col-span-3">
    <div class="flex items-center gap-2">
      <span class="text-lg">{{ account.categoryIcon }}</span>
      <div>
        <div class="font-medium text-sm">
          {{ account.categoryName }} - {{ account.accountName }}
        </div>
        <div class="text-xs text-gray-500">
          币种: {{ account.currency }} | 持有人: {{ account.holderName }}
        </div>
      </div>
    </div>
  </div>

  <!-- 前3个月历史投入（只读） -->
  <div class="col-span-2 text-right text-sm text-gray-600">
    {{ formatCurrency(historyData[account.id]?.month3Deposit ?? 0) }}
  </div>
  <div class="col-span-2 text-right text-sm text-gray-600">
    {{ formatCurrency(historyData[account.id]?.month2Deposit ?? 0) }}
  </div>
  <div class="col-span-2 text-right text-sm text-gray-700 font-medium">
    {{ formatCurrency(historyData[account.id]?.month1Deposit ?? 0) }}
  </div>

  <!-- 本月投入输入框 -->
  <div class="col-span-2">
    <div class="relative">
      <span class="absolute left-2 top-2 text-gray-500 text-sm">
        {{ getCurrencySymbol(account.currency) }}
      </span>
      <input
        v-model="depositAmounts[account.id]"
        type="number"
        step="0.01"
        min="0"
        placeholder="0.00"
        class="w-full pl-6 pr-2 py-1.5 text-sm border rounded"
        @input="markAsChanged(account.id)"
      />
    </div>
  </div>

  <!-- 本月取出输入框 -->
  <div class="col-span-2">
    <div class="relative">
      <span class="absolute left-2 top-2 text-gray-500 text-sm">
        {{ getCurrencySymbol(account.currency) }}
      </span>
      <input
        v-model="withdrawalAmounts[account.id]"
        type="number"
        step="0.01"
        min="0"
        placeholder="0.00"
        class="w-full pl-6 pr-2 py-1.5 text-sm border rounded"
        @input="markAsChanged(account.id)"
      />
    </div>
  </div>
</div>
```

#### 3. 底部统计区

```html
<div class="px-4 py-4 border-t bg-gray-50">
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
    <div>
      <div class="text-xs text-gray-600 mb-1">本月总投入</div>
      <div class="text-2xl font-bold text-green-600">
        {{ formatCurrency(summary.totalDeposits) }}
      </div>
    </div>
    <div>
      <div class="text-xs text-gray-600 mb-1">本月总取出</div>
      <div class="text-2xl font-bold text-red-600">
        {{ formatCurrency(summary.totalWithdrawals) }}
      </div>
    </div>
    <div>
      <div class="text-xs text-gray-600 mb-1">净投入</div>
      <div class="text-2xl font-bold text-blue-600">
        {{ formatCurrency(summary.netDeposits) }}
      </div>
    </div>
  </div>
</div>
```

**统计说明**:
- **本月总投入**: 所有账户投入金额总和
- **本月总取出**: 所有账户取出金额总和
- **净投入**: 总投入 - 总取出

#### 4. 数据加载和保存逻辑

**数据加载**:
```javascript
// 1. 加载投资账户列表
async function loadInvestmentAccounts() {
  // 获取该家庭所有投资账户（is_investment = TRUE）
  const accounts = await fetchInvestmentAccounts(selectedFamilyId)
  // 按大类分组排序
}

// 2. 加载前3个月历史数据
async function loadHistoryData() {
  const periods = [
    getPreviousPeriod(transactionPeriod, 3),
    getPreviousPeriod(transactionPeriod, 2),
    getPreviousPeriod(transactionPeriod, 1)
  ]

  // 并行加载3个月的数据
  const results = await Promise.all(
    periods.map(period =>
      fetchTransactionsByPeriod(selectedFamilyId, period)
    )
  )

  // 只提取投入金额（DEPOSIT）用于显示历史趋势
  results.forEach((records, index) => {
    records.filter(r => r.transactionType === 'DEPOSIT').forEach(record => {
      historyData[record.accountId][`month${3-index}Deposit`] = record.amount
    })
  })
}

// 3. 加载本月数据
async function loadCurrentMonthData() {
  const records = await fetchTransactionsByPeriod(
    selectedFamilyId,
    transactionPeriod
  )

  // 分别填充投入和取出
  records.forEach(record => {
    if (record.transactionType === 'DEPOSIT') {
      depositAmounts[record.accountId] = record.amount
      depositRecordIds[record.accountId] = record.id
    } else if (record.transactionType === 'WITHDRAWAL') {
      withdrawalAmounts[record.accountId] = record.amount
      withdrawalRecordIds[record.accountId] = record.id
    }
  })
}
```

**保存逻辑**:
```javascript
async function saveAll() {
  const transactionsToSave = []
  const transactionsToDelete = []

  // 遍历所有账户
  changedAccounts.forEach(accountId => {
    const depositAmount = parseFloat(depositAmounts[accountId] || 0)
    const withdrawalAmount = parseFloat(withdrawalAmounts[accountId] || 0)

    // 处理投入记录
    if (depositAmount > 0) {
      transactionsToSave.push({
        accountId,
        transactionPeriod,
        transactionType: 'DEPOSIT',
        amount: depositAmount
      })
    } else if (depositRecordIds[accountId]) {
      // 有原记录但现在为0，删除
      transactionsToDelete.push(depositRecordIds[accountId])
    }

    // 处理取出记录
    if (withdrawalAmount > 0) {
      transactionsToSave.push({
        accountId,
        transactionPeriod,
        transactionType: 'WITHDRAWAL',
        amount: withdrawalAmount
      })
    } else if (withdrawalRecordIds[accountId]) {
      // 有原记录但现在为0，删除
      transactionsToDelete.push(withdrawalRecordIds[accountId])
    }
  })

  // 批量保存和删除
  await batchSaveTransactions(transactionsToSave)
  await batchDeleteTransactions(transactionsToDelete)
}
```

#### 5. 账户排序规则

**排序优先级**:
1. **按大类排序**: 股票投资 > 退休基金 > 数字货币（根据大类ID或自定义顺序）
2. **同大类内排序**:
   - 有历史投入记录的账户排在前面
   - 无历史记录的排在后面
   - 同级别内按账户ID排序

**示例**:
```
📈 股票投资
  ├─ Fidelity 401k (有历史记录，排最前)
  ├─ Charles Schwab (有历史记录)
  └─ New Account (无历史记录，排最后)

🏦 退休基金
  ├─ Vanguard IRA (有历史记录)
  └─ Traditional IRA (无历史记录)
```

#### 6. 交互特点

**与支出管理批量录入的区别**:
| 项目 | 支出管理 | 投资管理 |
|------|---------|---------|
| 列表项 | 支出子分类 | 投资账户 |
| 输入列数 | 1列（金额） | 2列（投入 + 取出） |
| 历史数据 | 前3月支出 | 前3月投入 |
| 类型标签 | 固定日常/大额不定期 | 无需显示 |
| 货币选择 | 单一货币 | 每账户独立币种 |
| 底部统计 | 总支出 + 分类占比 | 总投入 + 总取出 + 净投入 |

**注意事项**:
1. **多币种处理**: 每个账户有自己的币种，输入框前显示对应币种符号
2. **投入/取出分离**:
   - 投入金额 > 0 → 创建DEPOSIT记录
   - 取出金额 > 0 → 创建WITHDRAWAL记录
   - 一个账户可以同时有投入和取出，生成两条记录
3. **历史数据**: 只显示前3月的投入金额（不显示取出），用于参考定投趋势
4. **统计汇总**: 底部显示所有账户的总投入、总取出、净投入（需要进行币种转换或分币种统计）

### 3.3 分析-投资分析-年度投资

**页面路径**: `/analysis/investment/annual`

**功能描述**:
分析某一年的投资回报情况，计算投资回报率。

#### 3.3.1 筛选控制区
```
- 家庭选择
- 年份选择
- 币种选择（USD/CNY/All）
```

#### 3.3.2 年度汇总卡片

显示整体投资情况：

```
┌─────────────────────────────────────────────┐
│  年度投资汇总 - 2024年                        │
├─────────────────────────────────────────────┤
│  期初总值: $450,000  (2023-12-31)            │
│  期末总值: $523,450  (2024-12-31)            │
│  本年投入: $65,000                           │
│  本年取出: $12,500                           │
│  净投入: $52,500                             │
│  投资收益: $20,950                           │
│  投资回报率: 4.66%                           │
└─────────────────────────────────────────────┘
```

**计算公式**:
```
期初总值 = 上一年12月31日所有投资账户的资产记录总和
         （如果查询2024年，取2023-12-31的asset_records）

期末总值 = 本年12月31日所有投资账户的资产记录总和
         （如果是当年且未到年底，取最新日期的asset_records）

本年投入 = SUM(amount WHERE transaction_type = 'DEPOSIT' AND transaction_period LIKE '2024-%')
本年取出 = SUM(amount WHERE transaction_type = 'WITHDRAWAL' AND transaction_period LIKE '2024-%')
净投入 = 本年投入 - 本年取出

投资收益 = 期末总值 - 期初总值 - 净投入
投资回报率 = (投资收益 / (期初总值 + 净投入/2)) × 100%
```

**注意**:
- 如果查询当年且未到年底，期末总值取最新可用日期的资产记录
- 回报率使用平均资本计算（期初资本 + 净投入的一半）

#### 3.3.3 大类分析 - 饼图

**显示内容**:
- 按资产大类汇总投资回报
- 饼图显示各大类的投资收益占比
- 点击饼图扇区可钻取到小类

**数据示例**:
```
股票投资: $12,500 (60%)
退休基金: $6,800 (32%)
数字货币: $1,650 (8%)
```

#### 3.3.4 小类分析 - 饼图（钻取）

**显示内容**:
- 点击大类后，显示该大类下各账户的投资收益
- 饼图显示账户占比

**数据示例**（股票投资大类下）:
```
Fidelity 401k: $8,500 (68%)
Charles Schwab: $4,000 (32%)
```

#### 3.3.5 账户详情 - 柱状图

**显示内容**:
- 选择某个账户后，显示该账户的年度月度趋势
- X轴：月份 (1-12月)
- Y轴左：投入/取出金额（柱状图）
- Y轴右：账户总值（折线图）

**柱状图**:
- 绿色柱：投入金额
- 红色柱：取出金额

**折线图**:
- 蓝色折线：账户总值（从asset_records获取每月末值）

#### 3.3.6 数据表格

显示所有投资账户的详细数据：

| 资产大类 | 账户名称 | 期初总值 | 期末总值 | 投入 | 取出 | 净投入 | 投资收益 | 回报率 |
|---------|---------|---------|---------|------|------|--------|---------|--------|
| 股票投资 | Fidelity 401k | $250,000 | $280,500 | $25,000 | $0 | $25,000 | $5,500 | 2.10% |
| 退休基金 | Vanguard IRA | $150,000 | $175,200 | $15,000 | $0 | $15,000 | $10,200 | 6.49% |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

**操作**:
- 点击账户名称可查看详情（跳转到账户详情页或弹窗）
- 支持导出Excel

## 4. API设计

### 4.1 投资交易API

**Base Path**: `/api/investments`

#### 4.1.1 获取投资账户列表
```
GET /investments/accounts?familyId={familyId}

Response:
{
  "success": true,
  "data": [
    {
      "accountId": 1,
      "accountName": "Fidelity 401k",
      "categoryId": 24,
      "categoryName": "股票投资",
      "categoryType": "STOCKS",
      "userId": 1,
      "userName": "张三",
      "currency": "USD",
      "institution": "Fidelity"
    }
  ]
}
```

#### 4.1.2 获取投资交易记录
```
GET /investments/transactions?familyId={familyId}&year={year}&month={month}

Response:
{
  "success": true,
  "data": [
    {
      "id": 1,
      "accountId": 1,
      "accountName": "Fidelity 401k",
      "categoryName": "股票投资",
      "userId": 1,
      "userName": "张三",
      "transactionPeriod": "2024-01",
      "transactionType": "DEPOSIT",
      "amount": 2500.00,
      "currency": "USD",  // 从asset_accounts获取
      "description": "月度定投"
    }
  ]
}
```

#### 4.1.3 创建投资交易
```
POST /investments/transactions

Request:
{
  "accountId": 1,
  "transactionPeriod": "2024-01",
  "transactionType": "DEPOSIT",
  "amount": 2500.00,
  "description": "月度定投"
}

Response:
{
  "success": true,
  "message": "投资交易记录创建成功",
  "data": { ... }
}

注：后端从account_id自动获取user_id和currency信息
```

#### 4.1.4 批量保存投资交易
```
POST /investments/transactions/batch

Request:
{
  "familyId": 1,
  "transactionPeriod": "2024-01",
  "transactions": [
    {
      "accountId": 1,
      "deposits": 2500.00,
      "withdrawals": 0,
      "description": "月度定投"
    },
    {
      "accountId": 2,
      "deposits": 1500.00,
      "withdrawals": 500.00,
      "description": "调仓操作"
    }
  ]
}

Response:
{
  "success": true,
  "message": "批量保存成功",
  "data": {
    "created": 3,
    "updated": 1
  }
}
```

#### 4.1.5 更新投资交易
```
PUT /investments/transactions/{id}

Request: (同创建)

Response:
{
  "success": true,
  "message": "投资交易记录更新成功"
}
```

#### 4.1.6 删除投资交易
```
DELETE /investments/transactions/{id}

Response:
{
  "success": true,
  "message": "投资交易记录删除成功"
}
```

### 4.2 投资分析API

**Base Path**: `/api/investments/analysis`

#### 4.2.1 获取年度投资汇总
```
GET /investments/analysis/annual/summary?familyId={familyId}&year={year}&currency={currency}

Response:
{
  "success": true,
  "data": {
    "year": 2024,
    "currency": "USD",
    "beginningValue": 450000.00,
    "endingValue": 523450.00,
    "totalDeposits": 65000.00,
    "totalWithdrawals": 12500.00,
    "netDeposits": 52500.00,
    "investmentReturn": 20950.00,
    "returnRate": 4.66
  }
}
```

#### 4.2.2 获取大类投资分析
```
GET /investments/analysis/annual/by-category?familyId={familyId}&year={year}&currency={currency}

Response:
{
  "success": true,
  "data": [
    {
      "categoryId": 24,
      "categoryName": "股票投资",
      "categoryIcon": "📈",
      "beginningValue": 250000.00,
      "endingValue": 280500.00,
      "totalDeposits": 35000.00,
      "totalWithdrawals": 0,
      "netDeposits": 35000.00,
      "investmentReturn": 12500.00,
      "returnRate": 4.66
    }
  ]
}
```

#### 4.2.3 获取账户投资分析
```
GET /investments/analysis/annual/by-account?familyId={familyId}&year={year}&currency={currency}&categoryId={categoryId}

Response:
{
  "success": true,
  "data": [
    {
      "accountId": 1,
      "accountName": "Fidelity 401k",
      "categoryName": "股票投资",
      "beginningValue": 150000.00,
      "endingValue": 168500.00,
      "totalDeposits": 25000.00,
      "totalWithdrawals": 0,
      "netDeposits": 25000.00,
      "investmentReturn": 5500.00,
      "returnRate": 3.35
    }
  ]
}
```

#### 4.2.4 获取账户月度趋势
```
GET /investments/analysis/annual/monthly-trend?accountId={accountId}&year={year}

Response:
{
  "success": true,
  "data": [
    {
      "month": 1,
      "period": "2024-01",
      "deposits": 2500.00,
      "withdrawals": 0,
      "accountValue": 152500.00
    },
    {
      "month": 2,
      "period": "2024-02",
      "deposits": 2500.00,
      "withdrawals": 0,
      "accountValue": 155200.00
    }
  ]
}
```

## 5. 业务逻辑说明

### 5.1 投资账户识别

**规则**:
- 资产账户所属的资产大类的`is_investment`字段为TRUE
- 在投资管理相关页面，只显示is_investment=TRUE的账户
- 关联关系：account_id → asset_accounts → user_id → family_members → family_id

**数据库查询**:
```sql
-- 获取某个家庭的所有投资账户
SELECT
    a.*,
    c.name as category_name,
    c.icon as category_icon,
    u.name as user_name
FROM asset_accounts a
JOIN asset_categories c ON a.category_id = c.id
JOIN users u ON a.user_id = u.id
JOIN family_members fm ON u.id = fm.user_id
WHERE c.is_investment = TRUE
  AND fm.family_id = ?
  AND a.is_active = TRUE
```

### 5.2 投资回报率计算

**时间跨度**:
- 期初：上一年12月31日（如查询2024年，取2023-12-31）
- 期末：本年12月31日（如果是当年且未到年底，取最新可用日期）

**获取期初期末值**:
```sql
-- 期初值（上一年12月31日）
SELECT SUM(amount) as beginning_value
FROM asset_records
WHERE account_id IN (投资账户ID列表)
  AND record_date = '2023-12-31'

-- 期末值（本年12月31日或最新日期）
SELECT SUM(amount) as ending_value
FROM asset_records
WHERE account_id IN (投资账户ID列表)
  AND record_date = (
    SELECT MAX(record_date)
    FROM asset_records
    WHERE account_id IN (投资账户ID列表)
      AND record_date <= '2024-12-31'
  )
```

**计算投入取出**:
```sql
-- 本年投入
SELECT SUM(amount) as total_deposits
FROM investment_transactions
WHERE account_id IN (投资账户ID列表)
  AND transaction_type = 'DEPOSIT'
  AND transaction_period LIKE '2024-%'

-- 本年取出
SELECT SUM(amount) as total_withdrawals
FROM investment_transactions
WHERE account_id IN (投资账户ID列表)
  AND transaction_type = 'WITHDRAWAL'
  AND transaction_period LIKE '2024-%'
```

**回报率公式**:
```
投资收益 = 期末总值 - 期初总值 - 净投入
平均资本 = 期初总值 + 净投入 / 2
投资回报率 = (投资收益 / 平均资本) × 100%
```

**注意事项**:
- 如果期初值为0且净投入 > 0，回报率 = (投资收益 / 净投入) × 100%
- 如果平均资本 <= 0，回报率标记为 N/A

### 5.3 多币种处理

**规则**:
- 交易记录的币种从资产账户继承（不单独存储）
- 每个资产账户有固定的currency字段
- 分析时支持按币种汇总或转换为统一币种
- 如果选择"All"币种：
  - 使用汇率表转换为基准货币（USD）
  - 复用现有的exchange_rates表和汇率转换逻辑

**查询示例**:
```sql
-- 获取交易记录的完整信息（包括user_id, family_id, currency等）
SELECT
    t.*,
    a.user_id,
    a.currency,
    a.account_name,
    u.name as user_name,
    fm.family_id
FROM investment_transactions t
JOIN asset_accounts a ON t.account_id = a.id
JOIN users u ON a.user_id = u.id
JOIN family_members fm ON u.id = fm.user_id
WHERE t.transaction_period LIKE '2024-%'
```

### 5.4 批量录入逻辑

**保存规则**:
1. 检查该期间是否已有记录
2. 如果投入金额 > 0，创建/更新DEPOSIT记录
3. 如果取出金额 > 0，创建/更新WITHDRAWAL记录
4. 如果金额为0或空，不创建记录（已存在的记录保持不变）

**更新策略**:
```javascript
// 伪代码
for each account in form:
  existing_deposits = find_transaction(account, period, 'DEPOSIT')
  existing_withdrawals = find_transaction(account, period, 'WITHDRAWAL')

  if form.deposits > 0:
    if existing_deposits:
      update(existing_deposits, amount: form.deposits)
    else:
      create(account, period, 'DEPOSIT', form.deposits)

  if form.withdrawals > 0:
    if existing_withdrawals:
      update(existing_withdrawals, amount: form.withdrawals)
    else:
      create(account, period, 'WITHDRAWAL', form.withdrawals)
```

## 6. 界面原型

### 6.1 管理-投资管理-分类与记录

**说明**: 此页面完全参照"支出管理-分类与记录"的布局设计（详见第3.1节），以下是简化版wireframe。

```
┌─────────────────────────────────────────────────────────────┐
│  投资管理 - 分类与记录              [选择家庭: Family 1 ▼]  │
│  管理投资账户，按大类查看投资趋势和交易记录                   │
├─────────────────────────────────────────────────────────────┤
│  📈 股票投资  │  🏦 退休基金  │  💎 数字货币    ← 大类Tab    │
├─────────┬───────────────────────────────────────────────────┤
│ 账户列表│  账户详情                                          │
│ (3列)  ├───────────────────────────────────────────────────┤
│         │  📈 股票投资 - Fidelity 401k      [+ 添加交易]    │
│ Fidelity│  持有人: 张三 | 币种: USD                         │
│ 401k    ├─────────────────┬─────────────────────────────────┤
│ $152.5K │  投资趋势       │  交易历史                       │
│ 12条记录│  [6月][1年]...  │  期间  │类型│金额    │操作     │
│         │                 │ ────────┼────┼────────┼─────   │
│ Charles │  [折线图        │ 2024-01│投入│ $2,500│编辑 删除│
│ Schwab  │   +柱状图]      │ 2024-02│投入│ $2,500│编辑 删除│
│ $85.2K  │                 │ 2024-03│取出│ $1,000│编辑 删除│
│         │                 │                                 │
│ ─────── │                 │                                 │
│ 总计    │                 │                                 │
│ $250.5K │                 │                                 │
│ 3个账户 │                 │                                 │
└─────────┴─────────────────┴─────────────────────────────────┘
```

**关键特征**:
- **横向大类Tab**: 顶部横向显示投资大类，点击切换
- **三列布局**: 左侧账户列表(3列) + 右侧详情区(9列)
- **账户列表**: 显示账户名称、最新总值、记录数，无添加按钮
- **详情区**: 账户信息卡片 + 两列等宽布局（趋势图 + 交易列表）
- **未选中时**: 显示"请选择投资账户"提示

### 6.2 管理-投资管理-批量录入

**说明**: 此页面完全参照"支出管理-批量录入"的布局设计（详见第3.2节），以下是简化版wireframe。

```
┌─────────────────────────────────────────────────────────────┐
│  批量录入投资交易                                             │
├─────────────────────────────────────────────────────────────┤
│  [家庭: Family 1 ▼]  [月份: 2024-02]        [保存全部]      │
├─────────────────────────────────────────────────────────────┤
│  账户            │ 11月  │ 12月  │ 01月  │ 2024年02月       │
│                  │(投入) │(投入) │(投入) │ 投入   │ 取出   │
│ ──────────────────┼───────┼───────┼───────┼────────┼────────│
│ 📈 Fidelity 401k │ $2.5K │ $2.5K │ $2.5K │[$2500] │[$   0] │
│ 📈 Charles       │ $1.0K │ $1.0K │ $1.0K │[$1000] │[$   0] │
│ 🏦 Vanguard IRA  │ $1.5K │ $1.5K │ $1.5K │[$1500] │[$   0] │
│ 💎 Coinbase      │   $0  │   $0  │ $500  │[$   0] │[$ 500] │
├─────────────────────────────────────────────────────────────┤
│  本月总投入: $5,000   本月总取出: $500   净投入: $4,500     │
└─────────────────────────────────────────────────────────────┘
```

**关键特征**:
- **横向滚动表格**: 前3月历史 + 本月投入/取出双列输入
- **历史参考**: 只显示前3月投入金额，帮助用户了解定投趋势
- **双输入框**: 每个账户有投入和取出两个输入框
- **底部统计**: 显示总投入、总取出、净投入三项汇总
- **多币种**: 每个账户显示自己的币种符号

### 6.3 分析-投资分析-年度投资

```
┌──────────────────────────────────────────────────────────────┐
│  投资分析 > 年度投资                                           │
├──────────────────────────────────────────────────────────────┤
│  [家庭选择 ▼] [年份: 2024 ▼] [币种: USD ▼]                    │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │  年度投资汇总 - 2024年                                  │ │
│  │  期初总值: $450,000  →  期末总值: $523,450             │ │
│  │  本年投入: $65,000  |  本年取出: $12,500               │ │
│  │  净投入: $52,500  |  投资收益: $20,950                 │ │
│  │  📊 投资回报率: 4.66%                                   │ │
│  └────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│  大类分析                                    账户分析（钻取）  │
│  ┌─────────────┐                         ┌──────────────┐  │
│  │             │                         │              │  │
│  │  [饼图]     │  点击饼图扇区 →         │   [饼图]     │  │
│  │  股票60%    │                         │  401k 68%    │  │
│  │  退休32%    │                         │  Schwab 32%  │  │
│  │  数字8%     │                         │              │  │
│  └─────────────┘                         └──────────────┘  │
├──────────────────────────────────────────────────────────────┤
│  月度趋势（选择账户后显示）                                   │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  [柱状图 + 折线图]                                      │ │
│  │  绿柱：投入  红柱：取出  蓝线：账户总值                  │ │
│  └────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│  详细数据表格                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │大类│账户│期初│期末│投入│取出│净投入│收益│回报率│          │ │
│  │... │... │... │... │... │... │... │... │...  │          │ │
│  └────────────────────────────────────────────────────────┘ │
│  [📥 导出Excel]                                              │
└──────────────────────────────────────────────────────────────┘
```

## 7. 开发任务拆分

### 7.1 数据库迁移
- [ ] 创建数据库迁移文件：V007__create_investment_management.sql
- [ ] 修改asset_categories表，添加is_investment字段
- [ ] 更新现有数据，标记投资类资产
- [ ] 创建investment_transactions表

### 7.2 后端开发
- [ ] 创建Model: InvestmentTransaction.java
- [ ] 创建Repository: InvestmentTransactionRepository.java
- [ ] 创建DTO:
  - InvestmentTransactionDTO.java
  - InvestmentAccountDTO.java
  - InvestmentAnnualSummaryDTO.java
  - InvestmentCategoryAnalysisDTO.java
  - InvestmentAccountAnalysisDTO.java
  - InvestmentMonthlyTrendDTO.java
- [ ] 创建Service:
  - InvestmentTransactionService.java
  - InvestmentAnalysisService.java
- [ ] 创建Controller:
  - InvestmentTransactionController.java
  - InvestmentAnalysisController.java

### 7.3 前端开发
- [ ] 创建API模块: src/api/investment.js
- [ ] 创建投资管理页面:
  - src/views/investments/InvestmentRecords.vue（分类与记录）
  - src/views/investments/InvestmentBatchEntry.vue（批量录入）
- [ ] 创建投资分析页面:
  - src/views/analysis/InvestmentAnalysis.vue（年度投资分析）
- [ ] 更新路由配置: src/router/index.js
- [ ] 更新侧边栏导航: src/components/Sidebar.vue

### 7.4 测试
- [ ] 创建测试数据脚本
- [ ] 单元测试
- [ ] 集成测试
- [ ] UI测试

## 8. 注意事项

1. **数据一致性**:
   - 投资交易记录与资产记录要保持一致
   - 删除资产账户时，级联删除相关投资交易记录

2. **性能优化**:
   - 投资分析可能涉及大量计算，考虑缓存策略
   - 批量操作使用事务确保原子性

3. **扩展性**:
   - 预留字段支持未来功能（如股票代码、交易手续费等）
   - 保持与现有支出管理模块的设计一致性

4. **用户体验**:
   - 批量录入页面自动加载现有记录，方便修改
   - 投资分析提供多维度视图，支持钻取
   - 图表交互友好，支持导出数据

5. **安全性**:
   - 投资交易记录按家庭隔离
   - API权限验证确保数据安全

## 9. 后续扩展功能（可选）

以下功能可在基础功能完成后考虑：

1. **持仓明细**:
   - 记录具体股票代码、份额、成本价
   - 支持分红、拆股等操作

2. **投资组合分析**:
   - 资产配置建议
   - 风险评估
   - 再平衡提醒

3. **对比分析**:
   - 与市场基准对比（如S&P 500）
   - 年度对比、账户对比

4. **定投计划**:
   - 设置定投计划
   - 自动提醒录入

5. **税务支持**:
   - 资本利得计算
   - 税务报表生成
